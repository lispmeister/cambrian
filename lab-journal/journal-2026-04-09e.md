# Lab Journal — 2026-04-09e: Post-Review Hardening Beads Implemented

## Session Goals
1) Implement the follow-up bead set created from the 2026-04-09 review
2) Close security and integrity gaps in Supervisor/Test Rig execution
3) Bring spec and code back into tighter synchronization with regression tests

---

## Scope Executed

Implemented and closed:

- `cambrian-j1yb` (P0): Mount reverse-run baseline artifact read-only/isolated
- `cambrian-rs6h`: Align BO persistence schema between spec and code
- `cambrian-ls3g`: Skip base-spec re-evaluation on BO resume
- `cambrian-i6sd`: Enforce true append-only semantics in generation updates
- `cambrian-0jod`: Harden Test Rig sandbox profile
- `cambrian-wxmm`: Add focused regression tests for new pre-test gates
- `cambrian-h64w`: Single-source runtime system prompt from spec
- `cambrian-cwzq`: Add measured-impact section to journal workflow

Epic `cambrian-0naj` closed after child bead completion.

---

## Code and Spec Changes

### 1) Reverse-run integrity + sandbox hardening

In `supervisor/supervisor.py`:

- Reverse-run now executes against a temporary workspace copy, not a direct mount of the historical promoted artifact
- Added container hardening controls to Test Rig and reverse-run containers:
  - `SecurityOpt: ["no-new-privileges:true"]`
  - `CapDrop: ["ALL"]`
- Existing `NetworkMode: "none"` behavior preserved

### 2) Generation store append-only semantics

In `supervisor/generations.py`:

- `append_only` updates no longer overwrite existing keys
- post-terminal annotations now truly append-only for listed keys
- logging now distinguishes applied vs skipped fields

### 3) BO loop resume correctness

In `supervisor/bo_loop.py`:

- base-spec evaluation now runs only on fresh BO runs
- resumed runs (with reloaded observations) skip base re-evaluation to avoid duplicate observation rows and budget distortion

### 4) Prompt single-source behavior

In `supervisor/prime_runner.py`:

- Added `extract_system_prompt()` and `resolve_system_prompt()`
- Runtime now extracts the canonical system prompt from CAMBRIAN-SPEC-005 (§ Fresh generation prompt)
- Falls back to local default prompt only if extraction fails

### 5) Test Rig baseline edge-case handling

In `test-rig/test_rig.py`:

- `run_baseline_check()` now returns `None` when all baseline contracts are malformed, preventing false-positive baseline pass signaling

### 6) Spec and journal updates

In `spec/CAMBRIAN-SPEC-005.md`:

- Version bump to `0.18.0`
- Documented runtime prompt extraction as canonical behavior
- Updated BO JSONL persistence schema to match implementation
- Added explicit resume rule: skip base re-eval when resuming from persisted observations
- `best-spec-meta.json` output schema updated to include `budget_used`

In `spec/BOOTSTRAP-SPEC-002.md`:

- Version bump to `0.14.0`
- Added mandatory hardening controls (`no-new-privileges`, `CapDrop: ALL`) for Test Rig containers
- Clarified reverse-run must use isolated copy (or otherwise immutable baseline source)

In `lab-journal/TEMPLATE.md` and `lab-journal/index.md`:

- Added and required a "Hypothesis vs Measured Impact" section for future entries

---

## New/Expanded Tests

### Added files

- `tests/test_test_rig_gates.py`
  - syntax-gate detection (`run_syntax_check`)
  - structlog anti-pattern lint detection (`run_structlog_lint`)
  - malformed baseline contracts handling (`run_baseline_check`)
- `tests/test_prime_runner.py`
  - prompt extraction from spec
  - fallback behavior when prompt block unavailable

### Expanded existing tests

In `supervisor/test_supervisor.py`:

- append-only non-overwrite behavior tests
- sandbox config assertions (`SecurityOpt`, `CapDrop`)
- reverse-run workspace-copy behavior assertion
- BO persistence schema key assertions
- BO resume no-duplicate-base-eval test

---

## Validation Results

Executed and passing:

```bash
uv run ruff check supervisor/supervisor.py supervisor/generations.py supervisor/bo_loop.py supervisor/test_supervisor.py test-rig/test_rig.py tests/test_test_rig_gates.py tests/test_prime_runner.py
uv run pytest supervisor/test_supervisor.py -k "append_only or no_api_key_in_env or baseline_reverse_run_uses_workspace_copy or bo_loop_persist_and_reload or bo_loop_resume_skips_base_spec_re_eval"
uv run pytest tests/test_test_rig_gates.py tests/test_prime_runner.py
uv run pytest tests/test_spec_compliance.py
```

Results:

- Ruff: clean
- Targeted supervisor suite: 6 passed
- New test files: 5 passed
- Spec compliance: 29 passed

---

## Hypothesis vs Measured Impact

| Change | Hypothesis (before run) | Measurement (after run) | Evidence / Notes |
|--------|--------------------------|--------------------------|------------------|
| Reverse-run artifact isolation | Eliminates historical artifact mutation risk | Implemented with workspace copy; config assertions pass | `test_run_baseline_reverse_run_uses_workspace_copy` |
| Sandbox tightening (`no-new-privileges`, `CapDrop`) | Reduce container privilege surface | Both Test Rig and reverse-run now enforce flags; tests pass | `test_run_test_rig_no_api_key_in_env` + reverse-run test |
| BO resume deduplication | Prevent duplicate baseline observations on restart | Resume path skips base eval; dedicated async test passes | `test_bo_loop_resume_skips_base_spec_re_eval` |
| Prompt source unification | Reduce prompt drift between code/spec | Runtime now reads prompt from spec with fallback path tested | `tests/test_prime_runner.py` |

---

## 30,000-Foot Status Overview (Spec-to-Code)

### Role split between the two active specs

- `BOOTSTRAP-SPEC-002` defines the environment and execution machinery (Supervisor, Test Rig, container policy, lifecycle, generation records, and infrastructure security constraints)
- `CAMBRIAN-SPEC-005` defines the organism/genome contract (Prime behavior, manifest invariants, generation semantics, prompt contract, and M2 evolution loop intent)

### Current code mapping

**Bootstrap-facing code (infrastructure):**

- `supervisor/supervisor.py` — container orchestration and generation lifecycle endpoints
- `test-rig/test_rig.py` — mechanical viability pipeline and diagnostics/fitness emission
- `supervisor/generations.py` — generation store semantics and update constraints
- `supervisor/baseline.py` — baseline battery extraction/loading and compatibility support

**Cambrian-facing code (organism/evolution):**

- `supervisor/prime_runner.py` — LLM-driven artifact generation, parsing, manifesting, and (now) spec-sourced system prompt resolution
- `supervisor/campaign.py` — campaign execution and promote/rollback decision loop
- `supervisor/bo_loop.py` — Stage-1 BO loop (single-objective viability optimization), persistence, and crash resume behavior
- `scripts/run_m2.py` — M2 entrypoint and best-spec outputs

### Where we are now

- Operationally, this is late hardening / pre-scale M2: core mechanisms are implemented and now being tightened for correctness, integrity, and drift resistance.
- The most recent work reduced key risk areas: reverse-run baseline mutation risk, container privilege surface, BO resume duplication behavior, append-only update semantics, and prompt drift between spec and runtime.
- Spec/code alignment is materially improved and backed by targeted regression + compliance tests.

### Strategic position

- We are past "can the loop run" and into "can the loop remain correct and auditable under repeated adversarial runs."
- Immediate priorities shift from plumbing to evolutionary quality/governance while preserving spec fidelity.
- Stage 2+ concepts remain explicitly deferred where budget/scale assumptions are not yet met.

---

**Signed:** lispmeister + AI assistant  
**Date:** 2026-04-09T13:10:00Z

**Participants & Tools:** GPT-5.3 Codex, Python 3.14.3, uv, pytest, ruff  
**Commit / Witness:** Working tree changes (uncommitted in this session) / beads `cambrian-0naj`, `cambrian-j1yb`, `cambrian-rs6h`, `cambrian-ls3g`, `cambrian-i6sd`, `cambrian-0jod`, `cambrian-wxmm`, `cambrian-h64w`, `cambrian-cwzq`  
**Related Specs / Beads:** CAMBRIAN-SPEC-005 v0.18.0, BOOTSTRAP-SPEC-002 v0.14.0

**Next journal entry:** journal-2026-04-10.md (or `journal-2026-04-09f.md` if same-day continuation)

**Continuation note:** Same-day continuation recorded in `journal-2026-04-09f.md` (M2 success rubric + auto-summary tooling).
