# Lab Journal — 2026-03-31e — Deep Spec-vs-Code Audit: Integration Gaps

## Session Goals
1) Comprehensive second-pass audit of all supervisor modules, test-rig, and both specs
2) Identify everything missing for M2 Stage 1 launch (cambrian-dzf)
3) Create beads for all findings

## Audit Method

Read every supervisor module, test_rig.py, both specs, and the Dockerfile in full. Cross-referenced every MUST/SHALL/MAY requirement against the code. Focused on integration gaps — modules that exist but aren't wired together.

## Findings

### Category 1: Blocking for M2 Stage 1

| Bead | Issue | Severity |
|------|-------|----------|
| cambrian-80h | Campaign runner never promotes/rollbacks — generations stuck in `tested` | P1 |
| cambrian-rc8 | `artifact-hash` is always empty string (MUST field) | P1 |
| cambrian-omj | `adaptive_tests` module is dead code — never called from BO loop | P1 |
| cambrian-axo | No M2 entry point script exists | P1 |

### Category 2: Spec Inconsistency

| Bead | Issue | Severity |
|------|-------|----------|
| cambrian-sww | CAMBRIAN-SPEC-005 says `/workspace/viability-report.json`; code + BOOTSTRAP say `/output` | P2 |

### Category 3: Code Correctness

| Bead | Issue | Severity |
|------|-------|----------|
| cambrian-lbz | No generation number >= 1 validation in /spawn | P2 |
| cambrian-gey | No duplicate generation spawn guard | P2 |
| cambrian-9i2 | `stages_completed_distribution` reads from possibly wrong field path | P2 |
| cambrian-n8f | Entanglement monitoring is dead code — never called | P2 |
| cambrian-0x6 | `completed` timestamp set on any update, not just terminal | P3 |
| cambrian-tch | No file locking on `generations.json` | P3 |
| cambrian-uf0 | No git concurrency control | P3 |
| cambrian-tif | Dockerfile copies single file, not directory per spec | P3 |

### Category 4: Fully Compliant (no gaps)

These areas were audited and found to match specs exactly:

- **Fitness metrics** — all 20 metrics from BOOTSTRAP-SPEC-002 §2.8 are emitted by `compute_fitness()`
- **Layer 1 spec vectors** — extraction, pre-read anti-tamper, evaluation order, $GENERATION substitution all match §2.9
- **Container isolation** — `/output` mount, `start_new_session=True`, process group kill with SIGTERM→SIGKILL all match §2.12
- **Timeouts** — build 300s, test 120s, start 30s, health 10s all match spec
- **Fail-fast pipeline** — skipped stages pre-populated with `{passed: false, duration_ms: 0}`
- **Diagnostics** — all MUST fields populated per §2.6
- **Supervisor HTTP API** — all 7 endpoints match spec (including the new spec-hash MUST enforcement)

### Category 5: Known Deferred (not Stage 1)

Not implemented and not needed for Stage 1:
- Type 2/3 mutations, MAP-Elites archive, Meta-Monitor, Test Tiers 1-3
- Layers 2-3 verification, Pydantic v2 models, `spec-lineage`, `CAMBRIAN_MODE` flag
- Crash recovery for orphaned `in_progress` records

## M2 Stage 1 Critical Path

After this audit, `cambrian-dzf` now depends on 4 new P1 blockers:

```
cambrian-dzf (M2 MVP)
  ← cambrian-80h (promote/rollback in campaign runner)
  ← cambrian-rc8 (artifact-hash computation)
  ← cambrian-omj (adaptive_tests integration)
  ← cambrian-axo (M2 entry point script)
```

These plus the existing infrastructure (BO loop, campaign runner, spec mutator, grammar, spec diff) form the complete integration surface for the first live evolution run.

## Test and Lint Status

No code changes in this session — audit only. 280 tests pass from previous session.

---

**Signed:** lispmeister + AI assistant
**Date:** 2026-03-31

**Participants & Tools:** Claude Sonnet 4.6, Python 3.14.3
**Commit / Witness:** (audit only — no code changes, beads created)
**Related Specs / Beads:** CAMBRIAN-SPEC-005 v0.14.0, BOOTSTRAP-SPEC-002 v0.10.0

**Next journal entry:** journal-2026-04-01.md (use TEMPLATE.md)
