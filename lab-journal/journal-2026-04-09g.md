# Lab Journal — 2026-04-09g: Architecture Diagram Refresh + README Integration

## Session Goals
1) Verify whether existing architecture diagrams still matched current M2 Stage 1 implementation
2) Update diagrams to reflect current runtime topology and security model
3) Regenerate publishable diagram assets and wire them into README

---

## Findings Before Changes

The existing diagrams in `spec/diagrams/` were directionally correct for early architecture, but stale in important ways:

- implied a long-running Prime container as active orchestrator (current flow is host-side `run_m2`/`bo_loop`/`campaign`/`prime_runner`)
- referenced older spec naming in sequence/architecture notes
- did not represent current `/output` isolation and hardening controls
- did not show campaign-aware polling and BO persistence/resume path

---

## Changes Implemented

### 1) Mermaid sources updated to current M2 Stage 1 architecture

Updated:

- `spec/diagrams/architecture-20260320.mmd`
- `spec/diagrams/generation-sequence-20260320.mmd`

Key updates captured:

- host-side M2 orchestration chain: `scripts/run_m2.py -> bo_loop.py -> campaign.py -> prime_runner.py`
- Supervisor endpoints and campaign polling behavior (`/versions?campaign-id=`)
- Test Rig report flow via isolated `/output`
- container guardrails (`NetworkMode=none`, no API key in Test Rig, `no-new-privileges`, `CapDrop=ALL`)
- BO observation persistence and completion artifacts (`best-spec.md`, `best-spec-meta.json`)

### 2) Rendering issues fixed

Two rendering issues were reported and resolved:

- PNG output was too low resolution: regenerated with larger width/scale
- SVG text missing: removed HTML label reliance in Mermaid flowchart source and switched labels to plain text/newlines

Also resolved one Mermaid sequence parse issue by simplifying note text punctuation.

### 3) Assets regenerated

Regenerated:

- `spec/diagrams/architecture-20260320.svg`
- `spec/diagrams/architecture-20260320.png`
- `spec/diagrams/generation-sequence-20260320.svg`
- `spec/diagrams/generation-sequence-20260320.png`

Resolution verification:

- architecture PNG: `4630 x 2514`
- generation-sequence PNG: `4768 x 2908`

### 4) README updated

Added a new architecture-diagrams section in `README.md` with:

- embedded current PNG diagrams
- links to `.mmd` and `.svg` sources
- explicit note that these diagrams supersede older M1-era long-running Prime-container mental models

---

## Validation

Executed diagram regeneration with Mermaid CLI after source updates; outputs regenerated successfully.

Checked:

- PNG dimensions (high-resolution)
- SVG includes text elements for readable labels

---

## Hypothesis vs Measured Impact

| Change | Hypothesis (before run) | Measurement (after run) | Evidence / Notes |
|--------|--------------------------|--------------------------|------------------|
| Refresh architecture diagrams to M2 Stage 1 | Reduce architecture drift between docs and runtime | Source diagrams now represent host-side orchestration and current security/reporting paths | `spec/diagrams/*.mmd` updated |
| Regenerate high-res artifacts | Improve readability in README/GitHub view | PNG assets regenerated at ~4.6k x ~2.5-2.9k resolution | `sips` dimension checks logged |
| Remove HTML label dependency for SVG text | Fix missing text in generated SVG | SVG outputs now contain text nodes and render labels correctly | regenerated `*.svg` outputs |

---

**Signed:** lispmeister + AI assistant  
**Date:** 2026-04-09T15:00:00Z

**Participants & Tools:** GPT-5.3 Codex, Mermaid CLI (`mmdc`)  
**Commit / Witness:** Working tree changes (uncommitted in this session)  
**Related Specs / Beads:** CAMBRIAN-SPEC-005 v0.18.0, BOOTSTRAP-SPEC-002 v0.14.0

**Next journal entry:** journal-2026-04-10.md (or `journal-2026-04-09h.md` if same-day continuation)
