# Lab Journal — 2026-03-23

## Session Context

SPEC-004 and BOOTSTRAP-SPEC-001 are complete. No running code exists yet. Project has 35 closed beads, all design-phase work. Today's focus: improve the bootstrap spec with innovations that accelerate the path to self-reproduction.

---

## Bootstrap Spec Evolution: BOOTSTRAP-SPEC-001 → BOOTSTRAP-SPEC-002

Produced BOOTSTRAP-SPEC-002 as a derivative of BOOTSTRAP-SPEC-001. Four major additions, each building on the last. The session was structured as a series of "what's the single most impactful addition?" prompts — forcing prioritization over breadth.

### Addition 1: Verification Contracts (§2.5)

**Problem:** The Test Rig's health-check stage hard-codes two checks (`GET /health` → 200, `GET /stats` → JSON with `generation`). In M2, when specs mutate, organisms may expose different endpoints. Every spec mutation would require a Test Rig code change.

**Solution:** A `contracts` array in the manifest — declarative HTTP checks that the organism declares and the Test Rig evaluates. Three match modes: `body` (exact), `body_contains` (partial), `body_has_keys` (structural). Variable substitution (`$GENERATION`) for dynamic values. Backward compatible — absent contracts fall back to hard-coded checks.

**Key insight:** The organism defines its own fitness criteria; the environment enforces them. This decouples organism evolution from environment code.

**Bead:** cambrian-05h

### Addition 2: Structured Diagnostics (§2.6)

**Problem:** Loom's 1.4% viability rate (1/72) was partly because the LLM had no structured feedback about WHY previous attempts failed. The viability report reduced everything to a boolean.

**Solution:** A `diagnostics` object in the viability report when `status` is `non-viable`. Contains: `stage`, `summary` (one-line, LLM-optimized), `exit_code`, `failures[]` (per-test with name/error/file/line), `stdout_tail` (last 100 lines), `stderr_tail` (last 100 lines). Per-stage formats defined for all five pipeline stages. Pytest failure extraction via `FAILED <file>::<test> - <error>` pattern.

**Key insight:** This turns the Test Rig from a Darwinian oracle (binary pass/fail, blind retry) into a Lamarckian teaching signal (structured failure context feeds into the next generation's prompt). The organism doesn't mutate randomly — it learns what went wrong.

**Bead:** cambrian-2hx

### Addition 3: Informed Retry (§2.7)

**Problem:** Diagnostics tell Prime WHAT failed, but Prime never sees the code that caused the failure. After rollback, `git branch -D gen-N` deletes the failed code. The LLM retries from scratch with no knowledge of its previous attempt.

**Solution:** Rollback now tags the failed branch (`gen-N-failed`) before deleting it. Generation records gain an `artifact_ref` field pointing to the tag. Prime reads its own previous failed code via `git show gen-N-failed:<file>` and includes it with diagnostics in the retry prompt. The LLM goes from "try again" to "here's what you wrote, here's what broke, fix it."

**Key insight:** Retries within `CAMBRIAN_MAX_RETRIES` are Lamarckian (informed by failure). Generations across spec versions (M2) are Darwinian (clean-room). Two evolutionary mechanisms at different timescales. Each retry still produces a COMPLETE artifact — no patching, no path dependence.

**Bead:** cambrian-hfh

### Addition 4: Fitness Vector (§2.8)

**Problem:** Viability is binary. All viable organisms are treated as equally good. When M2 arrives and you need to select between organisms, there's no historical data.

**Solution:** A `fitness` object in every viability report — 15 quantitative metrics across four dimensions: Speed (build/test/start/health durations), Correctness (test count, pass rate), Economy (token usage, source lines, dependency count), Robustness (test files, test lines, test-to-source ratio). All computed from data the Test Rig already collects. Zero additional test execution.

**Key insight:** The measurement apparatus is defined now; the selection policy is deferred to M2. Historical fitness data exists from Gen-1 onward — no retrofitting required.

**Bead:** cambrian-fy3

---

## SPEC-004 Updates

Brought SPEC-004 into compliance with BOOTSTRAP-SPEC-002 extensions:

1. Manifest schema: `contracts` (MAY)
2. Viability Report: `diagnostics` (MAY), Fitness Vector section with full schema
3. Generation Record: `artifact_ref` (MAY)
4. Rollback behavior: tag `gen-N-failed` before branch deletion
5. Retry semantics: informed retry SHOULD note
6. Example 2: updated to show tagging + informed retry flow

All changes are backward-compatible MAY/SHOULD fields. No existing MUST fields modified.

**Bead:** cambrian-r2m

---

## Architectural Insight: The Feedback Stack

The four additions form a coherent stack that transforms the retry loop:

```
§2.5 Contracts    — organism declares what it promises
§2.6 Diagnostics  — environment explains why it failed
§2.7 Informed Retry — organism reads its own failed code
§2.8 Fitness      — environment measures how good it is
```

Without this stack: spec → LLM → code → pass/fail → blind retry (Loom: 1.4% viability)
With this stack: spec → LLM → code → structured failure + source + metrics → informed retry

---

## Decision: Next Priority is CAMBRIAN-SPEC-005

The session reached a clear conclusion: stop polishing specs, write the genome. CAMBRIAN-SPEC-005 is the only critical-path item remaining before code can be generated.

SPEC-005 is uniquely challenging because it's a fixed point — a document that describes an organism that reads the document and produces itself. It's not a system spec (that's SPEC-004) or an implementation guide (that's BOOTSTRAP-SPEC-002). It's a prompt — the input to the LLM call that produces a living Prime.

SPEC-004's appendix already identifies which sections constitute the genome: overview, model, contracts, lifecycle, acceptance criteria, and examples. SPEC-005 distills and restructures this content for one reader: an LLM generating code.

---

## CAMBRIAN-SPEC-005: The Genome

Wrote the genome spec — 392 lines, the document an LLM reads to produce Prime.

### Design Decisions

**Self-contained over referential.** SPEC-005 inlines all schemas (manifest, Supervisor API, viability report) rather than pointing to SPEC-004. The LLM generating Prime won't have SPEC-004 in context. Every contract Prime must satisfy is spelled out in the genome itself.

**Prompt, not system spec.** SPEC-005 is structured for an LLM reader: what you're building → what contracts to satisfy → the generation loop step by step → how to call the LLM (including how to structure your own prompts) → acceptance criteria. No design principles section, no failure modes table — those are environment concerns in SPEC-004.

**Self-referential fixed point.** The document describes a general code generator. Self-reproduction emerges when Prime is given *this document* as input. The closing line makes this explicit: "You are the phenotype."

**File output format.** `<file path="...">content</file>` blocks — simple to parse with a regex, unambiguous boundaries, commentary outside blocks is ignored. The LLM writes files; Prime writes `manifest.json` separately (correct hashes require knowing all files first).

**Startup order matters.** Prime MUST bind port 8401 and serve `/health` before starting the generation loop. The Test Rig checks health during the start stage — if Prime blocks on LLM calls before binding the port, it times out and fails.

**File layout is SHOULD, not MUST.** Suggested layout (prime.py, generate.py, supervisor.py, manifest.py, models.py + tests) gives the LLM a starting point but doesn't constrain it. Different LLMs may organize code differently — what matters is that tests pass and contracts are satisfied.

**Informed retry prompt structure.** Two prompt modes: fresh generation (spec + history) and informed retry (spec + history + failed source code + diagnostics). The retry prompt shows the LLM its own previous failed code alongside structured diagnostics, enabling targeted fixes rather than blind retry.

**Bead:** cambrian-er5

---

## Beads Closed This Session

| Bead | Title | Priority |
|------|-------|----------|
| cambrian-05h | Add spec-derived verification contracts to BOOTSTRAP-SPEC-001 | P1 |
| cambrian-2hx | Add structured diagnostics to BOOTSTRAP-SPEC-002 | P1 |
| cambrian-hfh | Add informed retry via preserved failed artifacts to BOOTSTRAP-SPEC-002 | P1 |
| cambrian-r2m | Update CAMBRIAN-SPEC-004 with BOOTSTRAP-SPEC-002 extensions | P1 |
| cambrian-fy3 | Add fitness vector to viability report in SPEC-004 and BOOTSTRAP-SPEC-002 | P1 |
| cambrian-er5 | Write CAMBRIAN-SPEC-005 — the genome spec | P0 |

## Open Work

All design-phase specs are complete. Next critical path: implement Phase 0 (Supervisor + Test Rig) per BOOTSTRAP-SPEC-002.
