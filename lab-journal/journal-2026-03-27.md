# Lab Journal — 2026-03-27

## Topic: First Autonomous Gen-1 Run — Infrastructure Fixes and Genome Gaps

**Participants:** fix, Claude Sonnet 4.6
**Beads:** cambrian-iy3, cambrian-svf, cambrian-8eh
**Specs touched:** CAMBRIAN-SPEC-005 (v0.10.3 → v0.10.4), BOOTSTRAP-SPEC-002 (v0.8.4, no version bump)

---

## Context

First attempt at M1 self-reproduction: run Gen-1 autonomously against a live Supervisor and
watch it produce Gen-2. Gen-1 (`gen-1-new/`) is a human-written Prime that passed the full
test rig in the previous session. Today's run exposed two infrastructure bugs and a persistent
genome gap that prevented any offspring from reaching viability.

---

## 1. Infrastructure Bugs Found and Fixed

### 1.1 Streaming requirement (cambrian-svf)

Gen-1's `generate.py` used `client.messages.create()` with `max_tokens=32768`. The Anthropic
SDK rejected this with:

```
Streaming is required for operations that may take longer than 10 minutes.
```

**Fix:** Changed `call_llm()` to use `async with client.messages.stream() as stream:` with
`await stream.get_final_message()`. This is the correct pattern for large outputs; the spec
already stated "Streaming MUST be used" (BOOTSTRAP-SPEC-002 §… the system prompt note) but
the gen-1 implementation missed it. Fixed in `gen-1-new/src/generate.py` and manifest hash
updated.

### 1.2 Git state bug: `generations.json` branch conflict

The Supervisor's spawn handler creates a branch (`gen-N`), commits the artifact with
`git add -A`, and stays on that branch. When the test rig finishes and the next spawn is
called, `ensure_on_main()` runs `git checkout main` — which fails:

```
error: Your local changes to the following files would be overwritten by checkout:
    generations.json
```

Root cause: `generations.json` was committed on the `gen-N` branch (via `git add -A`), then
updated by the Supervisor after the test run. Checking out `main` would overwrite those
uncommitted updates.

**Fix:** Added `generations.json` to `.gitignore` in the artifacts repo and ran
`git rm --cached generations.json` to untrack it. Now:

- `git add -A` never stages `generations.json` (ignored)
- Branch switches never conflict on it — it persists in the working tree untouched
- The file accumulates generation history without git tracking, managed entirely by the
  Supervisor process

This also means `git branch -D gen-N` in the rollback path now works (we return to main
cleanly before the next spawn).

BOOTSTRAP-SPEC-002 does not document this requirement; a follow-up patch will add it.

---

## 2. Autonomous Run Results

Gen-1 ran with `CAMBRIAN_GENERATION=1`, workspace mounted at the artifacts root. The
Supervisor had `gen-1 promoted` in `generations.json`, so Prime targeted `gen-2` as the
first offspring.

| Generation | Outcome | Stage | Summary |
|-----------|---------|-------|---------|
| gen-2 | failed | test | `SyntaxError: unterminated string literal` in `tests/test_generate.py:29` |
| gen-3 | failed | test | 9 of 9 tests failed (same root cause) |
| gen-4 | failed (spawn) | — | Git state bug (fixed mid-run; gen-4 retried) |
| gen-4 (retry) | failed | test | `SyntaxError: unterminated string literal` in `tests/test_generate.py:29` (`close_tag = "` + actual newline) |
| gen-5 | failed | test | `SyntaxError` in `generate.py:24` — SYSTEM_PROMPT itself written as single-quoted string with embedded newlines |
| gen-6 | failed | build | Missing `requirements.txt` — parse failure produced incomplete output |
| gen-7 | failed | test | `SyntaxError: unterminated triple-quoted string literal` in `tests/test_generate.py:39` — trailing whitespace after opening `"""` |

Total LLM calls: 6 across 6 offspring. Cumulative tokens: ~205K. Estimated cost: ~$0.80–$1.00.

Max retries (3) were exhausted twice. The generation loop exited with `generations_produced=0`.

---

## 3. Root Cause: Python 3.14 String Literal Restrictions

The LLM consistently generated test code like this:

```python
close_tag = "
</file>"
```

Python 3.14 (following the deprecation in 3.12) treats an unescaped newline inside a
regular string literal as a `SyntaxError: unterminated string literal`. The correct form
is either:

```python
close_tag = "\n</file>"      # escaped newline
close_tag = """
</file>"""                   # triple-quoted
```

This failure appeared in `tests/test_generate.py`, which tests the `parse_files()` function.
That function extracts `<file path="...">content</file>` blocks from LLM responses. Writing
test strings for this naturally involves multi-line file content, which is where the LLM
went wrong.

The retry prompt included the full error message and failed source code, but Sonnet 4.6
repeated the same mistake across three separate retries. The model either lacks training
data on Python 3.14's strictness or doesn't connect the error message to the fix.

---

## 3a. Deep Analysis: What the LLM Is Trying to Quote

All failures in `test_generate.py` trace to the same functional need: the file contains tests
for `parse_files()`, which extracts `<file path="...">content</file>` blocks from raw LLM
response strings. To test this function, the test code must construct Python string literals
that *look like LLM responses* — i.e., strings containing `<file>` tags with multi-line content
inside them.

This creates a three-level quoting structure:

```
Python code → strings representing LLM output → strings representing Python code
```

The gen-1-new reference implementation (written by a human) avoids the problem with string
concatenation and `\n` escapes:

```python
response = (
    '<file path="src/a.py">a = 1</file>\n'
    '<file path="src/b.py">b = 2</file>'
)
```

The LLM, generating offspring, reaches for the "obvious" form — a string with a real embedded
newline — which was legal in Python ≤3.11 and is now a hard error in 3.14.

The failure modes across generations were:

| Gen | Location | Pattern | Error |
|-----|----------|---------|-------|
| 2, 4 | `test_generate.py:29` | `close_tag = "` + actual newline + `</file>"` | unterminated string literal |
| 5 | `generate.py:24` | SYSTEM_PROMPT written as single-quoted string with embedded newlines | unterminated string literal |
| 6 | build | `requirements.txt` absent — parse failure, incomplete output | build failure |
| 7 | `test_generate.py:39` | `"""Return:  ` with trailing whitespace after `"""` | unterminated triple-quoted literal |

Gen-5 is the most instructive: the LLM failed to apply the Python 3.14 rule to the
**SYSTEM_PROMPT constant itself** in `generate.py`. The SYSTEM_PROMPT is a multi-line
instruction string; the LLM wrote it as a single-quoted string with real newlines — the
exact pattern it was supposed to avoid. This means the problem isn't only about `<file>`
blocks in tests; it's a general failure to maintain the rule under the cognitive load of
generating a large codebase.

Gen-7 shows that retry feedback reached the LLM: it attempted triple quotes. But it
introduced trailing whitespace after the opening `"""`, which prevents Python from matching
the closing delimiter. The LLM understood the rule, applied it, and still got the syntax wrong.

**The structural issue:** the LLM's training data is overwhelmingly pre-3.12 Python, where
implicit line continuation inside string literals was legal (SyntaxWarning in 3.12, SyntaxError
in 3.14). The model has weak signal for this constraint. It can parrot the rule when prompted
but doesn't apply it reliably while generating hundreds of lines of code.

**Design question:** Could we avoid this class of failure by changing how test fixtures are
structured? Options:

1. **Spec provides a concrete fixture pattern** — give the LLM a copy-pasteable template rather
   than a rule to apply, e.g., "use this exact form for multi-line test strings in test_generate.py"
2. **Load fixtures from files** — store sample LLM response strings as `.txt` files in
   `tests/fixtures/`; no Python quoting issue
3. **Accept retry-convergence** — the rule is correct, question is whether 3 retries with
   Sonnet 4.6 is ever enough, or if escalation to a stronger model after N failures is needed

Option 2 trades a quoting problem for a fixture management problem (keeping the files in sync
with the parser spec). Option 3 is the minimal change — we already have the rule; we just need
the retry budget or escalation policy to match the failure rate.

---

## 4. Genome Gaps Identified

### 4.1 System prompt underspecifies Python 3.14

The SYSTEM_PROMPT says:

```
- The code must work in Python 3.14 inside a Docker container with a venv at /venv.
```

This states the requirement but not the constraints. "Works in Python 3.14" doesn't tell the
LLM what Python 3.14 enforces more strictly than 3.11 or 3.12. The spec needs to encode the
environmental fitness constraints precisely enough that the LLM can implement them correctly.

### 4.2 System prompt / spec divergence risk

The SYSTEM_PROMPT in `generate.py` is a hard-coded string, but it also appears verbatim in
CAMBRIAN-SPEC-005. These can diverge across generations. If the spec is updated but
offspring inherit the old system prompt from the parent's source code, the fix won't
propagate. For now both must be kept in sync manually; a future improvement is to have the
spec be the sole source of truth.

### 4.3 No retry escalation policy

After 3 identical failures, the system did nothing different. The spec doesn't define when
to escalate (use a stronger model, change the prompt strategy, or give up). This is an M2
concern — the organism should eventually be able to observe its own failure patterns and
mutate its genome to avoid them.

---

## 3b. Probe: Can Opus 4.6 Solve the Quoting Problem?

A focused probe script (`scripts/probe_opus_quoting.py`) was written to test the hypothesis
that the quoting failure is a model-capability issue solvable by escalation to Opus 4.6.

**Setup:** Send Opus 4.6 the exact SYSTEM_PROMPT from `gen-1-new/src/generate.py` (v0.10.4)
plus a focused task: "generate `tests/test_generate.py` with tests for `parse_files()`."
Validate the extracted file with `ast.parse()`. Run 5 attempts total.

**Results:**

| Attempt | Outcome |
|---------|---------|
| 1 | PASS (168 lines, syntax OK) |
| 2 | PASS (175 lines, syntax OK) |
| 3 | API error (Anthropic internal server error — not a model failure) |
| 4 | PASS (173 lines, syntax OK) |
| 5 | PASS (159 lines, syntax OK) |

4/4 generation attempts that completed produced syntactically valid Python 3.14. The probe
confirmed that Opus 4.6 correctly uses triple-quoted strings or `\n`-escaped strings when
constructing `<file>` block fixtures — exactly the pattern Sonnet 4.6 failed to apply.

**Conclusion:** The quoting failure is a model-capability issue. Escalating to Opus 4.6 is a
viable fix. This supports adding a retry-escalation policy: if Sonnet 4.6 fails N times on
the same generation, retry once with Opus 4.6.

**Second finding — parse_files() nested-tag bug:**

The initial probe used the same `FILE_PATTERN` regex as `parse_files()`:

```python
re.compile(r'<file path="([^"]+)">(.*?)</file>', re.DOTALL)
```

This non-greedy regex stops at the *first* `</file>` it encounters. When the LLM generates
`tests/test_generate.py`, the test fixture strings contain inner `<file path="...">...</file>`
patterns. The parser extracts only the fragment up to the inner `</file>` — a truncated,
invalid Python file. This means even a correctly-generated `test_generate.py` would be
corrupted by Prime's own extraction step.

This is an independent bug from the quoting issue. Even if Opus 4.6 generates perfect code,
the current `parse_files()` cannot correctly extract it. A fix to `parse_files()` is needed
alongside model escalation.

---

## 3c. Fix: Change Closing Delimiter to `</file:end>`

The `parse_files()` nested-tag bug was fixed by changing the closing delimiter from `</file>`
to `</file:end>`. This eliminates the ambiguity at the source: `</file:end>` cannot appear in
natural file content (the colon makes it invalid XML/HTML), so the non-greedy regex now
reliably terminates at the true block boundary.

The fix was minimal:
- `FILE_PATTERN` regex: `r'<file path="([^"]+)">(.*?)</file>'` → `(.*?)</file:end>`
- `SYSTEM_PROMPT` rule: `</file> blocks` → `</file:end> blocks`
- Spec (CAMBRIAN-SPEC-005 v0.10.4 → v0.10.5): updated format definition, regex description, system prompt, and output format examples
- `tests/test_generate.py`: all 11 fixture strings updated; new `test_nested_file_tags_in_content` test added

If the LLM generates `</file>` (without `:end`), `parse_files()` returns `{}` — a clean failure
that triggers a retry, not the silent corruption of the old truncation bug.

All 22 tests pass after the fix.

---

## 5. Fixes Applied This Session

1. `cambrian-artifacts/.gitignore` — added `generations.json`
2. `cambrian-artifacts/gen-1-new/src/generate.py` — streaming fix + Python 3.14 system prompt rules (v0.10.4) + `</file:end>` delimiter (v0.10.5)
3. `cambrian-artifacts/gen-1-new/tests/test_generate.py` — all fixtures updated to `</file:end>`, nested-tag test added
4. `cambrian-artifacts/gen-1-new/manifest.json` — spec-hash and artifact-hash updated
5. `spec/CAMBRIAN-SPEC-005.md` — v0.10.3 → v0.10.4 (Python 3.14 constraints) → v0.10.5 (`</file:end>` delimiter)
6. Synced all changes to `cambrian/tmp/gen-1-new/`

---

## Current State (end of morning session)

- Infrastructure is solid: streaming works, git state is clean, retry loop cycles correctly.
- Spec at v0.10.5: Python 3.14 string literal rules (v0.10.4) + `</file:end>` delimiter (v0.10.5).
- `parse_files()` nested-tag bug fixed; 22 tests pass.
- Probe confirms Opus 4.6 generates valid Python 3.14 (4/4 passes); Sonnet 4.6 does not.
- Next run deferred to this afternoon.
- `generations.json`: gen-1 promoted, gen-2 through gen-7 failed. Next offspring will be gen-8.

---

## Afternoon Session: Spec v0.11.0, Gen-1 Code Alignment, and First Successful Loop

**Beads:** cambrian-b82, cambrian-261, cambrian-0bw, cambrian-bsq, cambrian-jee, cambrian-244,
cambrian-78f, cambrian-k5h, cambrian-cwt, cambrian-0zs, cambrian-9j6

---

### 6. Spec v0.10.5 → v0.11.0

Three robustness patches applied to CAMBRIAN-SPEC-005 (cambrian-ib5):

1. **State-machine parser** — replaced the dotall regex description with line-by-line pseudocode.
   The regex was vulnerable to nested `<file>` tags in test fixtures; the state machine is immune.
   `parse_files()` now raises `ParseError` on unclosed blocks instead of silently returning `{}`.

2. **Parse repair loop** — on `ParseError`, Prime re-sends the raw response + error back to the
   same model (up to `CAMBRIAN_MAX_PARSE_RETRIES=2`) before consuming a generation retry. Handles
   LLM formatting failures without burning a generation slot.

3. **Model escalation** — `CAMBRIAN_MODEL` (default `claude-sonnet-4-6`) used on first attempt;
   `CAMBRIAN_ESCALATION_MODEL` (default `claude-opus-4-6`) used on retries. Spec also added
   `CAMBRIAN_MAX_PARSE_RETRIES` to the configuration table.

---

### 7. Gen-1 Code Brought Up to Spec

Gen-2 through gen-7 all failed under the old gen-1 code (pre-fix). After the spec patches,
gen-1's code had to be updated manually to match. Eight beads, all P1/P2:

| Bead | Change |
|------|--------|
| cambrian-b82 | `parse_files()` → state machine; `ParseError` class; `build_parse_repair_prompt()`; system prompt updated to `</file:end>` and Python 3.14 rules; 35 tests |
| cambrian-261 | Artifact hash: added null-byte separator between path and content bytes; tests |
| cambrian-0bw | Supervisor default URL: `localhost:8400` → `host.docker.internal:8400` |
| cambrian-bsq | `prime.py`: `MODEL` default → `claude-sonnet-4-6`; add `ESCALATION_MODEL` and `MAX_PARSE_RETRIES` |
| cambrian-jee | `prime.py`: `_parse_with_repair()` helper; model escalation in generation loop |
| cambrian-244 | Copy v0.11.0 spec into artifact; create missing `src/__init__.py`; recompute manifest hashes |
| cambrian-78f | `manifest.py`: `extract_contracts()` from spec; `spec_content` param in `build_manifest()`; default fallback contracts |
| cambrian-k5h | Supervisor threads `CAMBRIAN_MODEL`, `CAMBRIAN_ESCALATION_MODEL`, `CAMBRIAN_MAX_PARSE_RETRIES` etc. into container env |

After these fixes, all 47 gen-1 tests pass.

---

### 8. Architecture Clarification: Organism vs. Test Rig

A key misunderstanding was corrected during run setup. Previous attempts manually called
`POST /spawn` with `artifact-path: gen-1` and `generation: 3`. This is wrong for two reasons:

1. **The test rig is a verifier, not an executor.** It starts Prime, checks `/health` and
   contracts, then kills Prime. The generation loop never runs in this path.

2. **$GENERATION substitution uses `manifest["generation"]`.** The test rig substitutes
   `$GENERATION` in contracts with the value from the artifact's own `manifest.json`. Spawning
   gen-1 with `generation: 3` causes a mismatch: the contract expects `1`, Prime reports `3`.

The correct M1 flow:
- Run Gen-1 **directly** as a long-running Docker process, mounted against the artifacts root
- Gen-1's generation loop calls `/spawn` for Gen-2
- The Supervisor runs the **test rig** on Gen-2 (not Gen-1)
- Gen-1 polls for the result and calls `/promote` or `/rollback`

Docker command:
```
docker run --rm \
  -v /path/to/cambrian-artifacts:/workspace:rw \
  -e CAMBRIAN_GENERATION=1 \
  -e CAMBRIAN_SUPERVISOR_URL=http://host.docker.internal:8400 \
  -e CAMBRIAN_WORKSPACE=/workspace \
  --workdir /workspace/gen-1 \
  --entrypoint /bin/bash \
  cambrian-base:latest \
  -c "pip install -r requirements.txt -q && python -m src.prime"
```

Note: `CAMBRIAN_WORKSPACE=/workspace` maps to the artifacts root (not the gen-1 subdirectory),
so Prime writes gen-2 to `/workspace/gen-2` = `cambrian-artifacts/gen-2`, which matches what
the Supervisor resolves from `artifact-path: "gen-2"`.

---

### 9. Bugs Found During Run Setup

Three bugs surfaced before and during the first successful run:

**cambrian-cwt** — `stats-generation` contract hard-coded integer `generation` instead of
`"$GENERATION"` string. The manifest's default `stats-generation` contract was built with
`"generation": generation` (Python int). This causes contract failure when the test rig
substitutes `manifest["generation"]` (e.g., 2) and Prime correctly reports `2` — they match —
but when we were mistakenly testing gen-1 as gen-3, the mismatch exposed the bug. Fixed
in `manifest.py` to use `"$GENERATION"`. (Note: user later reverted this to the integer;
in the correct M1 flow the integers always match, so this only matters for manual testing.)

**cambrian-0zs** — `aiodocker.images.inspect(name)` returns 404 on Docker Desktop even when
the image exists (it appears in `list()`). Root cause: Docker Desktop uses a non-default
socket (`/Users/fix/.docker/run/docker.sock`) and image metadata may be stored differently.
Fixed the Supervisor's image existence check to use `list()` and match by `RepoTags`.

**cambrian-9j6** — `call_llm()` used `client.messages.create()` with `max_tokens=32768`.
The Anthropic SDK raises `"Streaming is required for operations that may take longer than
10 minutes"` for large `max_tokens` values. This bug was present in gen-1's code despite
having been fixed in `gen-1-new` in the morning session — the fix wasn't carried forward
during the code alignment work. Fixed by switching to `async with client.messages.stream()`.

---

### 10. First Successful Autonomous Loop

Gen-1 ran as the organism for the first time. The loop executed correctly end-to-end:

| Event | Time | Detail |
|-------|------|--------|
| Prime started | 12:14:35 | Serving `/health` and `/stats` on port 8401 |
| LLM call start | 12:14:35 | Sonnet 4.6, gen-2, retry=0 |
| LLM call complete | 12:17:21 | 11,553 input / 15,125 output tokens (~3 min) |
| Artifact written | 12:17:21 | 12 files written to `/workspace/gen-2` |
| Spawn accepted | 12:17:21 | Supervisor created `gen-2` branch, started test rig |
| Test rig complete | 12:17:33 | ~12 seconds — viable=False |
| Rollback | 12:17:33 | Tagged `gen-2-failed-3`, record updated |
| LLM call (retry) | 12:17:33 | Escalated to Opus 4.6, retry=1 |
| Opus 529 overload | 12:17:37 | Repeated 3× — all overloaded |
| Loop done | 12:17:49 | `generations_produced=0`, serving /health |

Gen-2 failure: 5 tests failed in `TestSupervisorClientHTTP` — TypeErrors and use of
`@unittest_run_loop` (deprecated in aiohttp 3.8+). 54/59 tests passed. Build was clean.
The retry prompt would have included full diagnostics; Sonnet 4.6 would likely have fixed it,
but Opus was unreachable.

Total tokens this loop: 26,678 (~$0.08 at Sonnet pricing).

---

### 11. Current State

- **The loop works.** Gen-1 generates, submits, tests, evaluates, and rolls back — autonomously.
- Gen-2 had fixable test failures (not a structural problem). The retry prompt carries full
  diagnostics; a second attempt with Sonnet should resolve them.
- Opus 4.6 is currently overloaded. Mitigation: set `CAMBRIAN_ESCALATION_MODEL=claude-sonnet-4-6`
  for the next run so all retries stay on Sonnet.
- `generations.json`: gen-1 promoted, gen-2 failed (rolled back). Next offspring will be gen-3.
- `generate.py` system prompt and genome spec now need a note about the streaming requirement
  so generated offspring don't repeat cambrian-9j6.
- Long-term: `@unittest_run_loop` deprecation warnings suggest the genome spec should
  mention that `pytest-asyncio` in AUTO mode makes the decorator unnecessary.

---

**Signed:** lispmeister + Claude Sonnet 4.6
**Date:** 2026-03-27

**Participants & Tools:** lispmeister + Claude Sonnet 4.6; Python 3.14, uv, Docker, aiodocker, aiohttp, claude-sonnet-4-6
**Commit / Witness:** f35d88e (cambrian-8eh + others — see entry body for full bead list)
**Related Specs / Beads:** CAMBRIAN-SPEC-005 v0.10.4+, BOOTSTRAP-SPEC-002; beads cambrian-8eh, cambrian-9j6, cambrian-iy3

**Next journal entry:** [journal-2026-03-29.md](journal-2026-03-29.md)
