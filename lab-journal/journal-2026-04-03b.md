# Lab Journal — 2026-04-03b: System Analysis + Spec/Code Gaps

## Session Goals
1) Read BOOTSTRAP-SPEC-002 then CAMBRIAN-SPEC-005 and map invariants
2) Audit Supervisor/Test Rig/M2 code for spec compliance and security issues
3) Capture inconsistencies and open beads for prioritized fixes

---

## Phase 1: Spec Review (Bootstrap → Cambrian)

Key mismatches and ambiguities identified:

| Area | Finding | Impact |
|------|---------|--------|
| Field naming | Spec says all JSON wire fields are kebab-case, but viability report schema uses `failure_stage` (snake_case). | Inconsistent contract expectations for report consumers. |
| Viability report path | BOOTSTRAP-SPEC-002 references `/workspace/viability-report.json` in earlier text, while CAMBRIAN-SPEC-005 requires `/output/viability-report.json` (isolated mount). | Spec ambiguity; code currently uses `/output`. |

---

## Phase 2: Code Audit Findings (Supervisor/Test Rig/M2)

### Critical

| Issue | Evidence | Bead |
|-------|----------|------|
| Artifact file write path traversal | LLM-provided paths are written without validation; absolute or `..` paths can escape artifact dir. | cambrian-mthk (P0) |
| `/spawn` path traversal guard bypass | `startswith()` string check on resolved path is bypassable by sibling paths. | cambrian-x27f (P0) |

### High

| Issue | Evidence | Bead |
|-------|----------|------|
| `$GENERATION` substitution for exact body contracts | Substitution is applied to response body instead of expected body, causing valid contracts to fail. | cambrian-g6ne (P1) |
| Non-viable `_error_record` schema mismatch | `failure_stage: "none"` on non-viable + missing `checks/diagnostics` fields. | cambrian-1iiz (P1) |

### Medium

| Issue | Evidence | Bead |
|-------|----------|------|
| Manifest file inclusions not enforced | `files` should include spec file + `src/__init__.py`; validator only checks `manifest.json`. | cambrian-jyou (P2) |
| Script-form start validation bypass | Wrapper commands can bypass python script-form gate. | cambrian-jpdc (P2) |
| Spec file path traversal | Spec file path from manifest is not constrained to `/workspace`. | cambrian-sr59 (P2) |
| Skipped test stage lacks `tests_run/tests_passed` | Schema requires these fields even on skip. | cambrian-ta2i (P2) |
| Missing/invalid manifest → empty `artifact-hash` | MUST field becomes empty string. | cambrian-u89r (P2) |
| Spec contradictions (kebab-case + report path) | Requires spec alignment updates. | cambrian-p8pa (P2) |

### Low / Hygiene

| Issue | Evidence | Bead |
|-------|----------|------|
| Fitness `total_duration_ms` sums skipped stages | Should omit unattempted stage metrics. | cambrian-46ve (P3) |
| Test Rig uses `print()` not structlog | Spec says structured logging for all components. | cambrian-ad3n (P3) |

---

## Outcomes

### Beads Created

- P0: cambrian-mthk, cambrian-x27f
- P1: cambrian-g6ne, cambrian-1iiz
- P2: cambrian-jyou, cambrian-jpdc, cambrian-sr59, cambrian-ta2i, cambrian-u89r, cambrian-p8pa
- P3: cambrian-46ve, cambrian-ad3n

### Immediate Recommendations

1) Fix the two P0 path traversal issues before running new campaigns.
2) Patch `$GENERATION` substitution bug to restore spec-vector and contract reliability.
3) Bring `_error_record` and skipped test fields into schema compliance to avoid poisoning fitness and retry logic.

---

**Signed:** lispmeister + AI assistant  
**Date:** 2026-04-03T08:32:00+04:00  

**Participants & Tools:** Claude Sonnet 4.6, Python 3.14, Docker Desktop  
**Commit / Witness:** N/A (analysis + bead creation only)  
**Related Specs / Beads:** CAMBRIAN-SPEC-005 v0.14.3, BOOTSTRAP-SPEC-002 v0.9.1, cambrian-mthk, cambrian-x27f, cambrian-g6ne, cambrian-1iiz, cambrian-jyou, cambrian-jpdc, cambrian-sr59, cambrian-ta2i, cambrian-u89r, cambrian-p8pa, cambrian-46ve, cambrian-ad3n

**Next journal entry:** journal-2026-04-04.md (or letter suffix if multiple sessions)
