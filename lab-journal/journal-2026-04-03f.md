# Lab Journal — 2026-04-03f: Run History Snapshot

## Session Goals
1) Record a concise summary of run history from `../cambrian-artifacts/generations.json`

---

## Run History Snapshot (from generations.json)

- Total generations recorded: 29 (gen 1–29)
- Outcomes: 7 promoted, 22 failed
- Promoted generations: 1, 4, 6, 8, 9, 10, 15
- Failure stages (count): manifest 10, test 5, health 3, start 3, build 1
- Latest 10 generations (20–29): all failed at manifest

---

## Notes

- `../cambrian-artifacts/generations.json` is the canonical history for generation numbering.

## Spec Mutator Walkthrough (Issue Review)

Summary of current logic:
- `type1_mutate` builds a prompt using campaign_summary (viability, failure distribution, fitness trend).
- Calls the LLM (streaming), extracts full spec text, and runs deterministic grammar validation.
- Accepts mutations if no *fatal* grammar violations are found; otherwise retries up to `_MAX_ATTEMPTS`.
- `propose_target_section` maps dominant failure stage → a single likely spec section.

Potential issues to investigate:
- Validation ignores non‑fatal violations that might still break intent.
- Response parsing only strips a top‑level code fence; any extra text can pollute the spec.
- Prompt forbids weakening MUST/MAY/SHALL, but validation doesn’t enforce semantics.
- Target section mapping is coarse; manifest failures may stem from non‑manifest sections.
- No explicit guard against truncated spec outputs.

---

## Infrastructure Proof Plan (Base Spec)

Goal: prove the trusted stack can implement the current base spec end‑to‑end before further mutation work.

Plan:
1) Baseline environment verification: ensure `cambrian-base:latest` is current and Supervisor/Test Rig start cleanly.
2) Gen‑0 sanity run: re-run a known hand‑crafted artifact through Supervisor → Test Rig to validate the verifier.
3) Base spec generation: run one generation using the current base spec with documented model/config.
4) End‑to‑end viability: confirm viable report at `/output/viability-report.json` and correct `generations.json` entry.
5) Repeatability: run 2–3 generations with the same base spec to show stability.
6) Audit evidence: capture commands, viability reports, and update lab journal with results.

Success criteria:
- At least one viable generation from the current base spec.
- All stages pass (manifest → build → test → start → health + spec vectors).
- `generations.json` entry is schema‑correct and includes hashes and viability record.
- Repeatability: one additional viable run or a documented reason for transient failure.

Notes:
- This proof must be completed before any further mutation work.

---

**Signed:** lispmeister + AI assistant  
**Date:** 2026-04-03T16:34:00+04:00  

**Participants & Tools:** GPT-5.2 Codex, Python 3.14  
**Commit / Witness:** N/A  
**Related Beads:** N/A

**Next journal entry:** journal-2026-04-04.md
