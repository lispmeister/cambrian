# Lab Journal — 2026-04-03c: P0–P2 Bug Fix Sweep

## Session Goals
1) Fix P0–P2 security and schema issues (path traversal, manifest validation, contract substitution)
2) Align specs on viability report path/naming
3) Add regression tests for all fixes

---

## Phase 1: Security + Manifest Hardening

| Fix | Location | Outcome |
|-----|----------|---------|
| LLM file write path traversal | `supervisor/prime_runner.py` | Added safe path resolution; absolute/.. paths rejected; new unit tests in `supervisor/test_prime_runner.py` |
| `/spawn` path traversal guard | `supervisor/supervisor.py` | Replaced prefix check with `Path.relative_to` containment; added sibling traversal payload test |
| Require `manifest.json` on spawn | `supervisor/supervisor.py` | `/spawn` now rejects missing/invalid manifest and invalid files list; tests updated to write minimal manifests |

## Phase 2: Test Rig Contract + Schema Fixes

| Fix | Location | Outcome |
|-----|----------|---------|
| `$GENERATION` in exact body contracts | `test-rig/test_rig.py` | Substitution applied to expected body; added test case |
| Required manifest files | `test-rig/test_rig.py` | Enforced `spec/CAMBRIAN-SPEC-005.md` and `src/__init__.py`; added tests |
| Script-form validation bypass | `test-rig/test_rig.py` | Regex now catches wrapper commands; added test for `uv run python src/prime.py` |
| Spec path traversal | `test-rig/test_rig.py` | `_find_spec_file` rejects paths escaping `/workspace`; added test |
| Skipped test fields | `test-rig/test_rig.py` | Skipped test stage now includes `tests_run/tests_passed`; test updated |
| Fitness duration sum | `test-rig/test_rig.py` | `total_duration_ms` now sums only completed stages; added test |

## Phase 3: Campaign Error Schema + Spec Alignment

| Fix | Location | Outcome |
|-----|----------|---------|
| Campaign error record schema | `supervisor/campaign.py` | `_error_record` now uses `_make_error_viability`; added lifecycle test |
| Viability report path + naming | `spec/BOOTSTRAP-SPEC-002.md`, `spec/CAMBRIAN-SPEC-005.md` | `/output` path clarified; viability report mixed naming explicitly documented |

---

## Tests Added/Updated

- New: `supervisor/test_prime_runner.py` (artifact path traversal)
- Updated: `tests/test_security.py`, `tests/test_lifecycle.py`, `supervisor/test_supervisor.py`
- Updated: `test-rig/test_test_rig.py` (contract substitution, manifest rules, spec path traversal, skipped test fields, total duration)

## Beads Closed

- P0: cambrian-mthk, cambrian-x27f
- P1: cambrian-g6ne, cambrian-1iiz
- P2: cambrian-jyou, cambrian-jpdc, cambrian-sr59, cambrian-ta2i, cambrian-u89r, cambrian-p8pa
- P3: cambrian-46ve

## Notes / Remaining Work

- Open: `cambrian-ad3n` (structlog in Test Rig or spec relaxation)
- Tests not run in this session (code changes only)

---

**Signed:** lispmeister + AI assistant  
**Date:** 2026-04-03T08:55:00+04:00  

**Participants & Tools:** Claude Sonnet 4.6, Python 3.14, Docker Desktop  
**Commit / Witness:** N/A (uncommitted fixes)  
**Related Specs / Beads:** CAMBRIAN-SPEC-005 v0.14.3+, BOOTSTRAP-SPEC-002 v0.9.1+, cambrian-mthk, cambrian-x27f, cambrian-g6ne, cambrian-1iiz, cambrian-jyou, cambrian-jpdc, cambrian-sr59, cambrian-ta2i, cambrian-u89r, cambrian-p8pa, cambrian-46ve

**Next journal entry:** journal-2026-04-03d.md (if more today)
